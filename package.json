{
  "name": "loop-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Static site of small HTML/CSS/JS apps built from app-ideas specs.",
  "scripts": {
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "prettier": "^3.4.2",
    "vitest": "^3.0.0"
  }
}
